# Review package: 45e0f3ad..2af2ead42f061587b7f7bc2948b1422474d64886

## Commits
2af2ead feat: add planDaysUsed to User model

## Files changed
 apps/backend/src/database/models/user.model.ts | 8 ++++++++  apps/backend/src/modules/auth/auth.service.ts  | 1 +  packages/shared-types/src/index.ts             | 2 ++  3 files changed, 11 insertions(+)

## Diff
diff --git a/apps/backend/src/database/models/user.model.ts b/apps/backend/src/database/models/user.model.ts index 2bdfd2e..3692284 100644 --- a/apps/backend/src/database/models/user.model.ts +++ b/apps/backend/src/database/models/user.model.ts @@ -34,20 +34,22 @@ export class User extends Model<    declare rescueCredits: CreationOptional<number>;    /** Temporary Pro window granted by a verified rewarded ad (Pro Pass). */    declare proPassUntil: Date | null;    /** Push quiet hours in LOCAL hours (0-23); null = defaults (22-8). */    declare quietStartHour: number | null;    declare quietEndHour: number | null;    /** Opt-out for the V2 satisfaction check-in loop (aftercare). Defaults on. */    declare feedbackEnabled: CreationOptional<boolean>;    /** Client-reported UTC offset in minutes; anchors "local day" math. */    declare tzOffsetMinutes: CreationOptional<number>; +  /** Lifetime days a free user has planned; free tier limit is 3. */ +  declare planDaysUsed: CreationOptional<number>;    declare readonly createdAt: CreationOptional<Date>;  }    export function defineUserModel(sequelize: Sequelize): typeof User {    User.init(      {        id: {          type: DataTypes.UUID,          primaryKey: true,          defaultValue: DataTypes.UUIDV4, @@ -115,20 +117,26 @@ export function defineUserModel(sequelize: Sequelize): typeof User {          type: DataTypes.BOOLEAN,          allowNull: false,          defaultValue: true,        },        tzOffsetMinutes: {          type: DataTypes.INTEGER,          allowNull: false,          defaultValue: 0,          validate: { min: -840, max: 840 },        }, +      planDaysUsed: { +        type: DataTypes.INTEGER, +        allowNull: false, +        defaultValue: 0, +        validate: { min: 0 }, +      },      },      {        sequelize,        modelName: 'User',        tableName: 'users',        underscored: true,        updatedAt: false,      },    );    return User; diff --git a/apps/backend/src/modules/auth/auth.service.ts b/apps/backend/src/modules/auth/auth.service.ts index 0f10abd..1d58194 100644 --- a/apps/backend/src/modules/auth/auth.service.ts +++ b/apps/backend/src/modules/auth/auth.service.ts @@ -148,16 +148,17 @@ export class AuthService {          sub: user.id,          email: user.email,          subscriptionTier: user.subscriptionTier as SubscriptionTier,        }),        expiresIn: env.JWT_EXPIRES_IN,        user: {          id: user.id,          email: user.email,          subscriptionTier: user.subscriptionTier as SubscriptionTier,          onboardingCompleted: user.onboardingCompleted as boolean, +        planDaysUsed: user.planDaysUsed as number,        },      };    }  }    export const authService = new AuthService(); diff --git a/packages/shared-types/src/index.ts b/packages/shared-types/src/index.ts index 123283e..9fc0a06 100644 --- a/packages/shared-types/src/index.ts +++ b/packages/shared-types/src/index.ts @@ -567,22 +567,24 @@ export interface ErrorResponse {    // ---------------------------------------------------------------------------  // Auth  // ---------------------------------------------------------------------------    export type SubscriptionTier = 'free' | 'pro';    export interface AuthUser {    id: UUID;    email: string; +  displayName?: string;    subscriptionTier: SubscriptionTier;    onboardingCompleted: boolean; +  planDaysUsed: number;  }    export interface AuthTokens {    accessToken: string;    expiresIn: string;    user: AuthUser;  }    // ---------------------------------------------------------------------------  // Ads & monetization
