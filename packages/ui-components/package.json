{
  "name": "@meal-rescue/ui-components",
  "version": "0.1.0",
  "private": true,
  "description": "Shared React Native components for Meal Rescue",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
