{
  "name": "@meal-rescue/ai-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "AI processing pipeline for Meal Rescue (vision, constraints, ranking)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "@meal-rescue/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
