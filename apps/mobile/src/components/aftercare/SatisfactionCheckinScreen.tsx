/**
 * PLACEHOLDER - owned by the v2/aftercare branch.
 *
 * The real Satisfaction Check-in screen lands on v2/aftercare and will replace
 * this body at merge time. mobile-core only guarantees that this route exists
 * and compiles so the navigator registration (SATISFACTION_ROUTE) is valid in
 * parallel. Returns null for now.
 */
export function SatisfactionCheckinScreen(_props: {
  route?: { params?: Record<string, unknown> };
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  navigation?: any;
}) {
  return null;
}
